# Sequential logic (planned)

This section will hold **clocked** building blocks: flip-flops, registers, simple pipelines, and reset strategies—aligned with *Digital Design and Computer Architecture* sequential-logic chapters.

## Planned units

- Synchronous register with active-low or synchronous reset
- Multi-bit register file (small) before full memory topics

## Status

Not yet implemented. Copy the layout from [00-combinational/01_mux](../00-combinational/01_mux/) when you add the first `rtl/` + `tb/` pair.
